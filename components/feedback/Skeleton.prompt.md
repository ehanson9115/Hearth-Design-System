**Skeleton** — pulsing placeholder lines for loading content. Rows stagger and shrink by default.

```jsx
<Skeleton rows={3} />
<Skeleton rows={4} widths={['100%','90%','70%','50%']} />
```

Pair with `StatusCard variant="loading"` for full-screen waits.
